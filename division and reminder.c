#include<stdio.h>
main()
{
    int a,b,c;
    /* a=divisor
       b=by which divison happens
       c=reminder*/
       printf("enter a,b");
       scanf("%d%d",&a,&b);
       c=a%b;
       printf("%d",c);
}
